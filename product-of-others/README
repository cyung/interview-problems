Given a list of integers, return a list containing the product of all other elements excluding the current index.

Example:
[2,3,4,5] returns [3*4*5, 2*4*5, 2*3*5, 2*3*4]

Requirements:
O(n) time
O(n) space
do NOT use division
